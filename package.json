{
  "name": "nostresso-vk-mini-app",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^7.20.0",
    "eslint-plugin-react": "^7.22.0",
    "react-scripts": "^4.0.2"
  },
  "dependencies": {
    "@babel/runtime": "^7.12.13",
    "@types/react": "^17.0.2",
    "@vkontakte/icons": "^1.76.0",
    "@vkontakte/vk-bridge": "^2.4.0",
    "@vkontakte/vkjs": "^0.20.0",
    "@vkontakte/vkui": "^4.1.0",
    "pigeon-maps": "^0.17.1",
    "prop-types": "^15.7.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "scripts": {
    "start": "cross-env PORT=10888 HTTPS=true react-scripts start",
    "tunnel": "npx @vkontakte/vk-tunnel --insecure=1 --http-protocol=https --ws-protocol=wss --host=localhost --port=10888",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npx vk-miniapps-deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
